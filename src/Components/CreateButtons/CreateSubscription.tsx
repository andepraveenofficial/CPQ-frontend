import React from 'react';
import { Button } from 'antd';

const CreateSubscription: React.FC = () => {
  return <Button type="primary">Create Subscription</Button>;
};

export default CreateSubscription;
